# Experiment log

| # | Date | Change (single variable where possible) | soft-OOF | gold-12 | Public LB |
|---|------|------------------------------------------|----------|---------|-----------|
| 1 | 08-19 | fold0 EffV2-S 256px 12sl 6ep, public hybrid labels | 0.834 | 0.871 | **0.872** |
| 2 | 08-19 | same, labels → v3c-blend rank-scale (BUG: uniform targets) | 0.821 | 0.816 | not submitted |
| 3 | 08-19 | same, labels → v3c-blend probability-scale | 0.849* | 0.869 | **0.880** |

| 4 | 08-19 | epochs 6→12 (same labels/config) | 0.841 (peak .8425 @ep9, ≤ 6ep's 0.849 — overfits soft labels past ~ep6) | 0.890* | not submitted |

| 5 | 08-20 | slices 12→16 (15 used, 5 triplets; batch 6) | **0.854** | 0.876 | candidate |
| 6 | 08-20 | laterality-aware aug: sagittal reversal + Med/Lat label swap | 0.835 ✗ | 0.851 | reverted — swap contradicts unmirrored cor/ax slots; coherent mirroring needs all planes flipped together |

*OOF measured against each run's own label set — not comparable across label versions; LB is the fair cross-run comparison.

Label sets vs gold-58 (ranking AUC): public best (hybrid_v4) 0.899 · our v3c alone 0.875 · v3c+2×hybrid blend 0.900-0.904.

Lessons: (1) label lift is real on hidden test (+0.008 LB, single-variable);
(2) training targets must be probability-scale, never percentile ranks;
(3) gold-12-per-fold is too small to rank models — use it only as a sanity anchor.
| v8 | 08-20 | exp-5 reproduction for clean latest weights (deterministic: OOF 0.8539) — **morning submission candidate** |
| 7 | 08-20 | submit-kernel slice bug: 16sl model fed 12 slices → LB 0.875; fixed (15 slices, training-identical sampling) → **LB 0.881** (new best; OOF +0.005 → LB +0.001, within public-split noise) |
| 8 | 08-20 | folds 1-4 trained (same config as best) — per-fold OOF: .854/.840/.847/.850/.847; merged 4,407-study OOF vs gold-58 (full anchor, n=58): **0.857** CI(.818-.891). Weak columns: MCL .78, Synovitis .79, PF OA .80 |
| 9 | 08-20 | 5-fold rank ensemble (16sl effv2s, v3c-blend labels) → **LB 0.897** (+0.016 over single fold 0.881; above the +0.005-0.01 expectation) |
| 10 | 08-20 | Labeler v4 iterations (smoke-only, ~0.6h GPU): v4 (PF OA/MCL/synovitis/fracture-negation anchors) 0.870 — fracture negation hurt Fracture (.765) and Contusion; v4b (negation reverted) 0.8749 ≈ v3c 0.8748; blend contribution +0.001-0.002 (noise). Full run NOT launched per pre-committed gate; ~5h quota preserved. PF OA anchor (+.05) worth carrying into v5 with a stronger model when quota resets |
| 11 | 08-20 | Distillation: targets = 0.5*teacher-OOF(5-fold ens) + 0.5*v3c-blend (targets gold 0.9166). Student single model OOF-vs-labels **0.876** (+0.022 over 0.854), gold-12 0.884 — largest single-model gain; efficiency-entry candidate |
| 12 | 08-20 | Distilled single model → **LB 0.900** — new overall best; single model ≥ 5-fold ensemble (0.897). Efficiency-entry thesis validated: full knowledge transfer at 1/5 inference cost |
| 13 | 08-20 | All-data distilled single model (4,407 studies, same targets) → **LB 0.902** — new overall best; +0.002 over fold-holdout distilled (0.900), extra 25% data helped |
| 14 | 08-21 | 10-epoch all-data distilled model (curve rose through ep9) → **LB 0.910** — new overall best (+0.008 over 6ep; longer training pays on clean distill targets) |
| 15 | 08-21 | v3d silence imputation (synovitis<-effusion when unmentioned): HURTS (0.900→0.897-0.899) — our labeler already emits calibrated non-zero severities for silence; community gain only applies to hard-zero labelers. Not adopted. | | | |
| 16 | 08-21 | 320px/16sl cache building as 2 CPU half-kernels (zero GPU; measured: single kernel would be 21.1GB > 20GB output cap) — ready for Saturday's high-res training | | | |
| 17 | 08-21 | CPU rank-blend of 10ep (0.910) + 6ep-holdout (0.900) distilled models → **LB 0.909** — blending in the weaker same-family member slightly dilutes the stronger one. Lesson: ensemble members must be comparable strength or genuinely diverse. Best remains 0.910. CPU-only inference of 2 models on 1,300 studies ran in ~2.5h (runtime datapoint) |

**Reconstruction note (08-30):** entries 18-23 below cover 08-23→08-26 Kaggle work that was submitted and scored but never logged/committed here — the session that ran it lost API access before it could pull results back and write to the repo. Recovered now from `kaggle competitions submissions` (authoritative for LB scores/dates) and each kernel's current `metrics.json`/log (soft-OOF); kernel slugs get overwritten on each push, so the code now under `kaggle/train-320*` etc. is the *final* version of each slug, not necessarily byte-identical to what produced every dated submission below. Distill targets throughout are `distill_targets_r2.csv` (round-2: retrain folds on round-1 distill targets → stronger teacher — the "distill round 2" item STRATEGY.md had queued next).

| 18 | 08-23 | 320px (up from 256px) all-data distilled EffV2-S, 24 slices, 10ep, mean-pool aggregation, distill-r2 targets → **LB 0.912** — resolution bump alone underperformed the 256px 0.910 baseline's next steps but confirmed the 320px cache/pipeline | | | **0.912** |
| 19 | 08-26 | Same 320px config, distill-r2 student retrained (`train-320s24`, soft-OOF 0.989) → **LB 0.915** — new best at the time | | | **0.915** |
| 20 | 08-26 | 320px 5-fold distill-r2 rank ensemble (`train-320-f0..f4`, per-fold soft-OOF 0.945-0.951, via `submit-ens-320`) → **LB 0.913** — *worse* than the single all-data model (0.915). Consistent with exp-17's lesson: at this label/arch maturity, ensembling near-identical single-seed members trades variance reduction for the all-data-training edge; the single model wins | | | 0.913 |
| 21 | 08-26 | `train-320-att`: BiGRU-over-slice-triplets + attention-MIL pooling (replaces mean-pool) + attention over the 4 series slots (replaces masked mean), same 320px/24sl/distill-r2 setup, soft-OOF 0.992 (architecture family later named **v2**) | | | not submitted alone |
| 22 | 08-26 | `train-v2`: production run of the v2 (BiGRU+attention) architecture, all-data, 320px, 24sl, distill-r2 targets, soft-OOF **0.992** | | | see #23 |
| 23 | 08-26 | v2 checkpoint submitted via `submit-v2` → **LB 0.922** — new overall best (+0.007 over exp-19), largest single jump since distillation itself. Aggregation (attention-MIL over slices+slots vs mean-pool) is the new highest-leverage lever, ahead of resolution | | | **0.922** |
| 24 | 08-29 | v2 architecture, 4/5 folds trained (`train-v2-f0..f3`; f4 not yet run), soft-OOF 0.940-0.944 per fold — 5-fold ensemble incomplete, not submitted; given exp-20's negative ensembling result at the plain-320 arch, a v2 5-fold ensemble is *not* pre-judged worth the remaining GPU-h without a CI-separated case for it | | | not submitted |
| 25 | 08-29 | v2 architecture at 384px (`train-v2-384`, all-data, 24sl, distill-r2, batch 1 vs 320's batch 2 — same 10 epochs), soft-OOF **0.961** — *lower* than the 320px v2's 0.992, tracking behind at every epoch (ep9: 0.961 vs 0.992). Caveat on both numbers: for FOLD=-1 (all-data) runs the code's own "val" is fold-0, which is *included in training* — the code comments it as "logging only, biased" (i.e. partly a train-fit readout, not a clean generalization estimate). Still comparable to each other (same contamination scheme both sides), so the 384px run trailing this hard at every epoch is real signal that it's undertrained/undertuned, not that 384px itself is worse. Batch-1 (vs 320's batch-2) is the leading suspect — **do not submit as-is**; match effective batch size (grad accumulation) before trusting it against the 0.922 LB best | | | not submitted |
| 26 | 08-30 | v2 @ 384px relaunch: 2-step gradient accumulation (effective batch 2, matching the 320px run) + LR back to 1e-4, otherwise identical to #25 → soft-OOF **0.954** (ep9), *not* better than #25's 0.961 — **batch-size hypothesis rejected**. Trajectory is smooth/monotonic-ish (no instability), just slower per-epoch than the 320px run at every epoch (ep5 0.949 vs 320's ep5 0.986). Reads as a genuine same-epoch-budget convergence gap at 384px, not an optimizer artifact. Two attempts (#25, #26) with no improvement and ~6h GPU each — **deprioritizing the 384px line** per the no-architecture-tourism rule; would need either many more epochs (expensive, unproven) or a resolution-appropriate LR/aug retune to reopen, neither cheap enough to justify without a CI-separated case | | | not submitted |
| 27 | 08-31 | **Gold-58 anchor for the LB-0.922 v2 checkpoint (`kaggle/validate-gold`, inference-only, ~11min on the near-empty weekly GPU quota):** macro-AUC **0.9218**, CI [0.892, 0.947], n_boot_valid=2000/2000. First real two-tier check on the v2 line — matches the public LB score almost exactly (0.9218 vs 0.922), a much tighter gold↔LB correspondence than the community-fit slope (LB≈0.825·gold+0.184) implied for earlier label/arch versions — either the anchor tracks better now or it's a lucky point estimate (CI is ±0.025-0.03, so treat the exact match as encouraging, not proof). Per-finding: strong ACL .973, Baker's .975, MCL .971, Medial OA .966, Medial Meniscus .963, Effusion .937, Fracture .940, Contusion .906; weak **Synovitis .839, Lateral OA .845, PF OA .876, Lateral Meniscus .872** — same weak set flagged back in exp-8/10 (PF OA, Synovitis), still unresolved | 0.9218(gold) | | 0.922 (existing) |
| 28 | 09-06 | Branch consolidation: the 08-29 (`plana-devam-et`) and 08-31 (`kaggle-api-key-continue`) branches had diverged from 08-21; merged here (08-31 docs kept; `train-v2-f4`, `submit-v2-384`, `build_distill_targets_r2.py` brought over). Ops lesson re-learned: a kernel pushed without `machine_shape: NvidiaTeslaT4` / `--accelerator NvidiaTeslaT4` lands on a P100 and dies at the first `.cuda()` (torch has no sm_60 kernels) — cost ~0.5 GPU-h across the first harness run and the first f4 attempt. Both re-launched on T4 | | | |
| 29 | 09-06 | **Efficiency runtime harness (`kaggle/runtime-harness`, T4, 100 train studies, exact submit-v2 pipeline, fp16 autocast):** sequential (as submit-v2 does it) **2.15 s/study → ~46.6 min at 1,300 studies** (io median 1.72 s, GPU forward median **0.15 s**, first forward 3.96 s warm-up, model load 1.6 s); 4-process DICOM preprocessing pool feeding the GPU **0.40 s/study → ~8.7 min**. Runtime term at hidden size: sequential 0.086 vs pooled 0.016 (0.01 AUC ≈ 0.022). Two conclusions: (1) inference is I/O-bound, the model is essentially free — the 24sl×320px v2 (and anything up to a few× its cost) fits the <15 min target with the pool; (2) the pooled pipeline is worth ≈0.03 AUC-equivalent on the efficiency score, more than any single modeling gain to date. Also: the LB-0.922 `submit-v2` kernel ran with **GPU off** (~105 s/study on CPU), so no efficiency-LB reading for it is meaningful. `kaggle/submit-v2-fast` = same checkpoint + pooled preprocessing + T4/fp16; validated on the 3-row public test (10 s wall, predictions match the CPU submit-v2 output to max abs diff 0.005 = fp16 tolerance), **awaiting submission approval** | | | pending approval |
| 30 | 09-06 | **v2 fold-4 trained** (`train-v2-f4`, T4, 4.01h, 320px/24sl, distill-r2 targets): soft-OOF **0.9460** at ep9 (plateau from ep6: .9457/.9452/.9460/.9460) — best of the v2 folds (f0 .944, f1 .941, f2 .9395). v2 5-fold set is now complete → full 4,407-study v2 OOF available for confident-learning cleanup and as the final-teacher ensemble; no ensemble submission planned (exp-20) | 0.946 | | |
| 31 | 09-06 | **`submit-v2-fast` submitted** (approved by team lead): same exp-23 checkpoint, 4-process DICOM pool + T4 fp16 → **LB 0.922**, identical to the CPU submission as expected (AUC unaffected; only the efficiency runtime term changes). Submitted 19:42, scored by 20:05 — ≈22 min wall including queue/scoring, consistent with the harness's ~9 min inference estimate (the API does not expose the rerun's own runtime; the host efficiency LB is the only readout). From here on every submission uses the pooled pipeline | | | **0.922** |
| 32 | 09-06 | **Supervision push, step 1 — per-finding diagnosis on gold-58** (all via `src/validate.py`): incumbent soft labels (v3c-blend) Synovitis .810 / Lateral OA .825 / PF OA .871 / Lat Meniscus .865; r2 targets .850/.830/.884/.894; v2 5-fold OOF .821/.793/.831/.801; our v3 labeler alone .787/.751/.755/.816. Read: Synovitis and Lateral OA are teacher-limited (labels are the ceiling), PF OA / Lat Meniscus are model-limited on top of mediocre labels; our own labeler is the weak member of the blend on all four. v3 full run cost: 4,400 reports in 5.6 h (4.6 s/report, batch 8, 200 max tokens) → thinking mode on full data (~8× tokens) is out | | | |
| 33 | 09-06 | **Labeler v5 smoke (`kaggle/labeler-v5`, Qwen3-8B, weak-four-only prompt with English evidence quotes, gold-58, 340 s):** v5 severity vs incumbent soft: PF OA .878 vs .871 (**blend .905**, +.034; our labeler alone .755→.878), Lateral OA .841 vs .825 (11 gold positives — noise-level), Lat Meniscus .863 vs .865 (neutral), **Synovitis .640 vs .810 — badly worse**: the "effusion without a synovial descriptor is NOT synovitis" rule is wrong for image-derived gold (gold prevalence 47%, report mentions it in <50% of cases; effusion is the best proxy — the mirror image of exp-15's training-side lesson). Synovitis dropped from v5. Cost 5.6 s/report → ~7 h full run, too expensive; evidence field + synovitis block removed for the full run (3 findings, ~60 output tokens), re-smoked first since the prompt changes. batch 16 OOMs on 2×T4 with this prompt; 8 fits | | | |
| 34 | 09-06 | **v5b re-smoke (3 findings, evidence field removed, 80 max tokens, 154 s, 2.5 s/report):** the gain vanished — PF OA .757 (v5a .878; incumbent .871), Lat Meniscus .822 (v5a .863), Lateral OA .789 (v5a .841). Grades/severities alone are back at v3 quality (.75-.82). **Reading: the English evidence quote is the mechanism** — it forces translate-then-label and a per-finding lookup before scoring, i.e. a cheap chain-of-thought; without it the 8B model scores from the whole report and confuses compartments/laterality. Next: v5c = evidence FIRST in the tuple ([evidence, g, s], ≤8 words) so the lookup precedes the score, 3 findings, 140 max tokens; smoke, then full run (~4.5 h) if PF OA ≥ .87 alone or blend ≥ .89 | | | |
| 35 | 09-06 | **v5c smoke (evidence FIRST, 3 findings, 275 s, 4.3 s/report):** PF OA .789, Lat OA .809, Lat Men .850 — v5a's .878 did not reproduce; three prompt variants span .76-.88 on 21 gold positives. Not CI-separable → per the pre-committed rule the 7 h full run is **not** launched; labeler line on the weak four closed for Qwen3-8B (total spend ~0.3 GPU-h). Lesson: gold-58 cannot rank prompt variants for a single finding; a labeler change needs a different gate (e.g. hand-checked evidence quotes per language) before it earns a full run | | | |
| 36 | 09-06 | **Distill round 3 targets** (`src/build_distill_targets_r3.py`): 0.5·v2 5-fold OOF + 0.5·v3c-blend → gold-58 **0.9266** vs r2 0.9237 (gate PASS; variants 0.5·oof+0.5·r2 = .9259, (oof+r2+soft)/3 = .9269 — all equivalent, kept the established recipe). Per-finding vs r2: Lat Men .904 (+.010), Syn .853 (+.003), PF OA .883 (=), Lat OA .824 (−.006). Uploaded as `distill_targets_r3.csv` to the private soft-labels dataset. Launched `train-v2-r3` (v2 arch, all-data, 320px/24sl, 10 ep, T4, ~5 h) — same +.003 target-gold step that r1→r2 turned into +.003 LB | | | pending |
| 37 | 09-07 | **`train-v2-r3` done** (4.23 h, T4): ep9 loss 0.4145, biased in-train soft-OOF 0.9919 — trajectory indistinguishable from train-v2 on r2 targets (loss 0.4136 / 0.992; losses are against different targets, so not strictly comparable). No training-side signal either way; only LB / gold can tell. `submit-v2-r3-fast` (pooled T4/fp16 pipeline, r3 checkpoint) prepared and validated on the 3-row public test — **awaiting submission approval** | | | pending approval |
| 38 | 09-07 | **Pivot (team lead: "GİDELİM"): efficiency-track plan = "fast 0.935"** — efficiency LB rewards runtime hard (a 0.936 sits at rank 4 above a 0.952 at rank 6), so the target is ~0.935 AUC at ≤4 min, not 0.95. Levers taken from the public authors' stated priorities (STRATEGY reality check). Launched **`train-v3`**: v2 encoder + BiGRU, but the shared triplet/slot attention + MLP replaced by 12 per-finding query tokens attending over all slot×triplet tokens (slot + position embeddings, sex token, per-finding linear readout; 1.5M head params). Single variable vs `train-v2-r3` (same 4×24 320px cache, r3 targets, 10 ep). CPU (free): **6-slot cache** `preprocess-320s6a/b` (Sag/Cor/Ax × FS/non-FS, 16 slices each = same I/O as 4×24, no duplicated series, empty slots masked) for the next step | | | pending |
| 39 | 09-07 | **Runtime harness v2 (cold cache, disjoint 60-study subsets per config, pool-4, T4):** 4×24@320 **0.79 s/study → ~17 min** (the 9 min in #29/#31 was a warm-cache artifact: the sequential pass had pulled the files into the OS page cache before the pooled pass); 6×16@320 0.86 (18.6 min); 4×16@320 0.65 (14.1); 4×12@320 0.61 (13.2); 6×12@320 0.79; 6×16@256 0.81; 4×24@256 0.62 (13.3). Encoder forward 0.05-0.09 s/study for all configs — the model is free. Reading: halving the slices only buys ~23%, so the floor is per-file header reads (every file of each chosen series is opened for position sorting, plus one header per series for slot selection) and cold disk latency, not pixel decode. Consequence: the speed lever is **concurrency** (more processes / per-series threads), not fewer slices — next harness measures pool 4/8/16 and 4 proc × 4 threads. Also: our LB-0.922 efficiency entry actually runs ~17 min, so it is currently paying ~0.014 AUC-equivalent in runtime | | | |
| 40 | 09-07 | **Concurrency harness (4×24@320, cold, disjoint subsets):** pool4 0.53 s/study (11.5 min) → pool8 **0.36 (7.9 min)** → pool16 0.36 (saturated; 4 vCPUs) → pool8×4thr 0.35; flat thread pools 0.85-0.89 (GIL). Study-subset variance is large (pool4 measured 0.79 on another subset in #39), so only within-run comparisons count. Reading: after pool8 the job is CPU-bound (pydicom header parse of every file in each chosen series for position sorting + pixel decode/resize), so the remaining lever is reading fewer files: order by filename/InstanceNumber when it matches position order and open only the sampled slices; take slot metadata from test_series.csv where trustworthy. Next harness measures exactly that | | | |
| 41 | 09-07 | **Harness v4 (pool8, cold, disjoint subsets):** filenames are SOP UIDs — filename order never matches position order (0/1106 series), but InstanceNumber was monotone with position in every inspected series. Cost if ordering were free (open only the sampled files): 4×24 **6.9 min** (vs 8.5 with the full header scan), 4×16 **4.7 min**, 4×12 **3.6 min**, 6×16 6.2 min; header scan alone costs ~1.6-3.3 min at hidden size. Plan: raw-byte scan of InstanceNumber (tag 0020,0013 in the first KBs) instead of pydicom for ordering, pydicom only for the sampled pixels; validate against pydicom on 200 studies, fall back per series when the scan fails or is non-monotone | | | |
| 42 | 09-07 | **Harness v5:** raw-byte InstanceNumber ordering agrees with pydicom position ordering in **1,686/1,686 series** (1,065 same direction, 621 reversed → direction fixed by reading IPP of the first and last file only). Cost (pool8, cold): 4×24 **6.9 min** (vs 8.6 with full header scan), 4×16 5.4, 4×12 5.2, 6×16 7.4, 6×12 6.7. Runtime is now a second-order term (7 min ≈ 0.006 AUC-equivalent); the 24-vs-16-slice choice is decided by AUC, not speed. Production inference recipe from here: pool8 + fast ordering + direction check + pydicom fallback per series | | | |
| 43 | 09-07 | **Harness v6:** direction-corrected fast loader reproduces the position-sorted volumes **exactly (120/120 studies, max uint8 diff 0)**; 4×24 7.3 min, 4×16 5.4 min at hidden size (pool8, cold). Adopted as the production inference path in `submit-v2-fast` (v2 checkpoint; the same file is the template for v3); validated on the 3-row public test: predictions identical to the previous version (max diff 0.0), 10 s wall. **Re-submitting the 0.922 checkpoint through it would cut the efficiency runtime term from ~17 to ~7 min (≈0.008 AUC-equivalent) — awaiting approval** | | | |
| 44 | 09-07 | 6-slot cache complete (both CPU halves). Launched **`train-v3-s6`**: v3 per-finding-attention model on 6 slots (Sag/Cor/Ax × FS/non-FS) × 15 slices (5 triplets; 90 images/study vs 96), r3 targets, 10 ep, T4 — single variable vs `train-v3` (slots). Runs concurrently with train-v3; week's GPU spend after both ≈ 20 h of 30. Ops note: never `kaggle kernels output` a cache kernel without `--file-pattern` — it pulls the whole multi-GB cache (caught and killed) | | | pending |
| 45 | 09-07 | **`train-v3` done** (4.33 h, T4, per-finding query-attention head, 4×24@320, r3 targets): ep9 loss **0.4126** — lowest of any run on r3 targets (train-v2-r3: 0.4145), biased in-train soft-OOF 0.9945 (vs 0.9919). Curve: .9104/.9490/.9669/.9777/.9855/.9888/.9913/.9937/.9943/.9945. A consistent but small training-side gain; LB/gold decide. `submit-v3-fast` prepared (fast loader + v3 Net) — awaiting approval. `train-v3-s6` still running | | | pending |
| 46 | 09-07 | **Gold-58 anchor, v3 checkpoint (`validate-gold-v3`):** macro **0.9277** CI [0.897, 0.952] vs v2's 0.9218 (+0.006, inside CI). Per finding v2→v3: ACL .973→.989, MCL .971→.968, Med Men .963→.968, **Lat Men .872→.927 (+.055)**, Med OA .966→.972, **Lat OA .845→.812 (−.033)**, PF OA .876→.889, Effusion .937→.964, Synovitis .839→.836, Baker's .975→.982, Contusion .906→.907, **Fracture .940→.918 (−.022)**. Confound: v3 trained on r3 targets, the 0.922 v2 on r2 — `validate-gold-r3` (v2 arch, r3 checkpoint) launched to attribute. `submit-v3-fast` validated (3 rows, 9 s) — **awaiting submission approval** | | 0.9277 | pending approval |
| 47 | 09-07 | **Attribution (`validate-gold-r3`, v2 arch on r3 targets): gold 0.9224** CI [0.891, 0.947] — r3 targets alone ≈ the r2 v2 (0.9218): distill round 3 bought nothing (as suspected in #37), so the v3 gain is the **head**: 0.9224 → 0.9277 (+0.005). Per finding r3-v2 → v3: Lat Men .888 → **.927**, Effusion .954 → .964, ACL .977 → .989, Baker's .969 → .982; Fracture .933 → .918, Med Men .977 → .968. Lateral OA is .814 in both r3 runs (vs .845 for the r2 v2) — a target/seed effect, not the head. Decision: v3 head adopted as the new baseline; `train-v2-r3` checkpoint will not be submitted (delete `submit-v2-r3-fast` from the queue) | | 0.9224 | not submitted |
| 48 | 09-07 | **`train-v3-s6` done** (4.16 h, T4; v3 head on 6 slots × 15 slices, cache 4407 confirmed): ep9 loss 0.4132 / in-train AUC 0.9941 vs train-v3's 0.4126 / 0.9945 — training-side equal or a hair behind (90 vs 96 images per study, 5 vs 8 triplets per slot). Gold-58 anchor (`validate-gold-v3s6`) and `submit-v3s6-fast` launched | | | pending |
| 49 | 09-07 | **Gold-58, v3-s6 (6 slots FS/non-FS × 15 slices): 0.9203** CI [0.888, 0.946] vs v3 (4×24) **0.9277** — the 6-slot layout does not help at equal image budget (Lat Men .906 vs .927, Synovitis .817 vs .836, Contusion .885 vs .907; PF OA/Med OA/Fracture equal). Inside CI, but no evidence for it and it costs slice depth (15 vs 24 per slot). **Not adopted**; v3 on 4×24 remains the candidate. `submit-v3s6-fast` validated (3 rows, 9 s) but not queued. Week's GPU spend ≈ 21 h of 30 | | 0.9203 | not submitted |
| 50 | 09-12 | **`submit-v3-fast` submitted** (approved; v3 head, gold 0.9277, fast loader). New GPU week: launched **`train-v3-s43`** (v3, identical config, SEED 43 — for a 2-seed average and a seed-variance read on gold) and the **32-slice 320px cache** (`preprocess-320s32a/b/c`, CPU thirds) for a deeper-slot v3 run (I/O cost at hidden size ~9 min per harness scaling) | | | pending |
| 51 | 09-12 | **v3 on LB: 0.923** (v2 0.922; +0.001, inside the 0.005 public-LB noise). The +0.005 gold-58 gain (0.9218 → 0.9277, lateral meniscus +.055) did not show on the public split. Read honestly: the per-finding head is not worse and is cheap, so it stays the baseline, but it is not the 0.01-class lever the public authors' claim suggested for our stack. Efficiency-LB effect: same AUC at ~7 min instead of ~17 min | | 0.9277 | **0.923** |
| 52 | 09-12 | `submit-v2-fast` v2 (0.922 checkpoint on the pool8 + fast-ordering path) submitted on approval — same AUC expected, runtime term ~7 min instead of ~17. Dominated by the v3 submission (#51: 0.923 at the same runtime); kept as a runtime datapoint only → **LB 0.922** | | | 0.922 |
| 53 | 09-12 | 32-slice 320px cache complete (3 CPU thirds). Launched **`train-v3-s32`**: v3 on 30 of 32 slices per slot (10 triplets; 120 images/study vs 96), single variable vs train-v3 (slice depth). ~25% more compute per step, time-guarded at 10.5 h. `train-v3-s43` (seed) still running | | | pending |
| 54 | 09-12 | **`train-v3-s43` done** (5.07 h — slower session; seed 43, otherwise = train-v3): ep9 loss 0.4127 / in-train AUC 0.9943 vs seed 42's 0.4126 / 0.9945 — training reproducible to the 3rd decimal. Gold anchor (`validate-gold-v3s43`) launched for the seed-variance read vs 0.9277 | | | pending |
| 55 | 09-12 | **Seed variance on gold-58: v3 seed 43 = 0.9217 vs seed 42 = 0.9277** (identical training curves to the 3rd decimal). The 0.006 seed spread equals the v3-over-v2 "gain" (#46), matching the LB read (+0.001, #51). Gold averages of member predictions (CPU, from the validate kernels' gold_preds.csv): v3 2-seed prob-avg 0.9256, rank-avg 0.9255, v3×2 + r3-v2 0.9259, all four checkpoints 0.9260 — averaging lands on the members' mean, no lift visible at n=58. **Rule from here: no single-seed gold read decides anything below 0.01; candidates are judged on LB (noise 0.005) or multi-seed gold.** `submit-v3x2-fast` (2-seed probability average, +0.15 s/study) prepared as a variance-reduction submission — awaiting approval | | | |
| 56 | 09-12 | **`train-v3-s32` done** (5.73 h; v3 on 30 slices/slot = 10 triplets): ep9 loss 0.4126 / in-train AUC 0.9942 — identical to v3 at 24 slices (0.4126 / 0.9945) and to seed 43. Slice depth 24→30 changes nothing on the training side at +32% step cost. Gold anchor (`validate-gold-v3s32`) launched | | | pending |
| 57 | 09-12 | **Gold-58, v3 on 30 slices: 0.9229** CI [0.891, 0.948] — same band as v3 seed 43 (0.9217) and r3-v2 (0.9224); only v3 seed 42 (0.9277) sits above, i.e. that one is the outlier. Slice depth 24→30: no gain, +32% cost. **Not adopted.** Family summary (all r3 targets, EffNetV2-S 2.5D): 0.920-0.928 gold, 0.922-0.923 LB across v2/v3/6-slot/32-slice/seeds — exhausted. Next: backbone swap (v4 = DINOv2-S, last 6 blocks fine-tuned, CLS⊕patch-mean @336, v3 head), smoke first | | 0.9229 | not submitted |
| 58 | 09-12 | **v4 DINOv2-S smoke OK** (300 studies, 1 ep, 3 min → ~44 min/epoch at full data; trainable 10.7M backbone + 2.6M head; ep0 in-train AUC 0.729 on 300 studies, sane). **Full `train-v4-dino` launched** (all data, 10 ep, ~7.3 h, T4): DINOv2-S last-6-blocks fine-tune @336 (CLS⊕patch-mean) under the v3 BiGRU + per-finding attention head, r3 targets — the first lever outside the EffNetV2-S family | | | pending |
